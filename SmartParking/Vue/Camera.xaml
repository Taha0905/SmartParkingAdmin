<Page x:Class="SmartParking.Camera"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="1040" d:DesignWidth="1500"
      Title="Camera">

    <Grid Background="#1E1E1E">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            <!-- Titre -->
            <TextBlock Text="🎥 Vidéosurveillance" Foreground="White" FontSize="40" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,40"/>

            <!-- Conteneur principal -->
            <Grid Width="1100" Height="650">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Zone vidéo -->
                <Border Grid.Row="0" Background="#2B2B2B" CornerRadius="15" BorderBrush="#3C3C3C" BorderThickness="2" Margin="20">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="🔴 Flux Vidéo en Direct" Foreground="White" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="0,20,0,10"/>
                        <Rectangle Width="800" Height="469" Fill="Gray" Opacity="0.2"/>
                        <TextBlock Text="(Aperçu vidéo ici)" Foreground="LightGray" FontSize="18" TextAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Panneau de contrôle -->
                <Border Grid.Row="1" Background="#2B2B2B" CornerRadius="15" Padding="20" Margin="226,20,226,20" BorderBrush="#3C3C3C" BorderThickness="2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Bouton Activer -->
                        <Button Content="▶️ Activer Caméra" FontSize="18" Foreground="White" Background="#27AE60" Width="220" Height="50"
                                BorderBrush="Transparent" Cursor="Hand" Margin="10" />
                        <!-- Bouton Arrêter -->
                        <Button Content="⏹ Arrêter Caméra" FontSize="18" Foreground="White" Background="#E74C3C" Width="220" Height="50"
                                BorderBrush="Transparent" Cursor="Hand" Margin="10" />
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
