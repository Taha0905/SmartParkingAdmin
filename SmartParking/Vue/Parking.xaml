<Page x:Class="SmartParking.Vue.Parking"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="1040" d:DesignWidth="1500"
      Title="Parking">

    <Page.Resources>
        <Style x:Key="ParkingSpaceStyle" TargetType="Border">
            <Setter Property="Background" Value="#2E3A59"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderBrush" Value="#4FD1C5"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ParkingSpaceOccupiedStyle" TargetType="Border">
            <Setter Property="Background" Value="#4A1A1A"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderBrush" Value="#F56565"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2E3A59"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>

        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#A0AEC0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>

        <Style x:Key="ParkingNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A1E2C" Offset="0"/>
                <GradientStop Color="#121420" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Background="#252A41" Padding="20" Margin="0,0,0,20">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <material:PackIcon Kind="CarSide" Width="32" Height="32" Foreground="#4FD1C5" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Text="VISUALISATION DU PARKING" 
                               Foreground="White"
                               FontSize="24" FontWeight="SemiBold"
                               VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.2" Color="Black"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <TextBlock Text="État en temps réel" Foreground="#4FD1C5" FontSize="14" 
                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Contenu principal -->
        <Grid Grid.Row="1" Margin="20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Zone de parking -->
            <Border Grid.Column="0" Background="#2E3A59" CornerRadius="12" Padding="30" Margin="0,0,20,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Titre de la section -->
                    <Grid Grid.Row="0" Margin="0,0,0,25">
                        <TextBlock Text="Plan du Parking" Style="{StaticResource SectionTitleStyle}"/>
                        <Border Background="#4FD1C5" Height="3" Width="80" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,25,0,0"/>
                    </Grid>

                    <!-- Grille des places de parking -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Colonne gauche : Places 1, 2, 3 -->
                        <Border x:Name="Place1Border" Grid.Row="0" Grid.Column="0" Style="{StaticResource ParkingSpaceStyle}" BorderBrush="White">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon x:Name="Place1Icon" Kind="Car" Width="40" Height="40" Foreground="#48BB78" HorizontalAlignment="Center"/>
                                <TextBlock Text="Place 1" Style="{StaticResource ParkingNumberStyle}"/>
                                <TextBlock x:Name="Place1Status" Text="Disponible" FontSize="12" Foreground="#48BB78" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>


                        <Border x:Name="Place2Border" Grid.Row="1" Grid.Column="0" Style="{StaticResource ParkingSpaceStyle}" BorderBrush="White">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon x:Name="Place2Icon" Kind="Car" Width="40" Height="40" Foreground="#48BB78" HorizontalAlignment="Center"/>
                                <TextBlock Text="Place 2" Style="{StaticResource ParkingNumberStyle}"/>
                                <TextBlock x:Name="Place2Status" Text="Disponible" FontSize="12" Foreground="#48BB78" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="Place3Border" Grid.Row="2" Grid.Column="0" Style="{StaticResource ParkingSpaceStyle}" BorderBrush="White">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon x:Name="Place3Icon" Kind="Car" Width="40" Height="40" Foreground="#48BB78" HorizontalAlignment="Center"/>
                                <TextBlock Text="Place 3" Style="{StaticResource ParkingNumberStyle}"/>
                                <TextBlock x:Name="Place3Status" Text="Disponible" FontSize="12" Foreground="#48BB78" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>


                        <!-- Allée centrale -->
                        <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Background="#1E293B" CornerRadius="4" Margin="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon Kind="ArrowUpDown" Width="24" Height="24" Foreground="#A0AEC0"/>
                                <TextBlock Text="ALLÉE" FontSize="10" Foreground="#A0AEC0" HorizontalAlignment="Center" Margin="0,5,0,0" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>

                        <!-- Colonne droite : Places 4, 5, 6 -->
                        <Border x:Name="Place4Border" Grid.Row="0" Grid.Column="2" Style="{StaticResource ParkingSpaceStyle}" BorderBrush="White">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon x:Name="Place4Icon" Kind="Car" Width="40" Height="40" Foreground="#48BB78" HorizontalAlignment="Center"/>
                                <TextBlock Text="Place 6" Style="{StaticResource ParkingNumberStyle}"/>
                                <TextBlock x:Name="Place4Status" Text="Disponible" FontSize="12" Foreground="#48BB78" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>


                        <Border x:Name="Place5Border" Grid.Row="1" Grid.Column="2" Style="{StaticResource ParkingSpaceStyle}" BorderBrush="White">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon x:Name="Place5Icon" Kind="Car" Width="40" Height="40" Foreground="#48BB78" HorizontalAlignment="Center"/>
                                <TextBlock Text="Place 5" Style="{StaticResource ParkingNumberStyle}"/>
                                <TextBlock x:Name="Place5Status" Text="Disponible" FontSize="12" Foreground="#48BB78" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border x:Name="Place6Border" Grid.Row="2" Grid.Column="2" Style="{StaticResource ParkingSpaceStyle}" BorderBrush="White">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <material:PackIcon x:Name="Place6Icon" Kind="Car" Width="40" Height="40" Foreground="#48BB78" HorizontalAlignment="Center"/>
                                <TextBlock Text="Place 4" Style="{StaticResource ParkingNumberStyle}"/>
                                <TextBlock x:Name="Place6Status" Text="Disponible" FontSize="12" Foreground="#48BB78" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                    </Grid>
                </Grid>
            </Border>

            <!-- Panneau de statistiques -->
            <StackPanel Grid.Column="1">
                <!-- Titre des statistiques -->
                <TextBlock Text="Statistiques" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,15"/>

                <!-- Places disponibles -->
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <material:PackIcon Kind="CheckCircle" Width="20" Height="20" Foreground="#48BB78" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Places disponibles" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                        <TextBlock x:Name="PlacesDispoText" Text="6" Style="{StaticResource StatNumberStyle}" Foreground="#48BB78"/>
                    </StackPanel>
                </Border>

                <!-- Places occupées -->
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <material:PackIcon Kind="CloseCircle" Width="20" Height="20" Foreground="#F56565" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Places occupées" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                        <TextBlock x:Name="PlacesOccupeesText" Text="0" Style="{StaticResource StatNumberStyle}" Foreground="#F56565"/>
                    </StackPanel>
                </Border>

                <!-- Taux d'occupation -->
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <material:PackIcon Kind="Percent" Width="20" Height="20" Foreground="#4FD1C5" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Taux d'occupation" Style="{StaticResource StatLabelStyle}"/>
                        </StackPanel>
                        <TextBlock x:Name="TauxOccupationText" Text="0%" Style="{StaticResource StatNumberStyle}" Foreground="#4FD1C5"/>
                    </StackPanel>
                </Border>

                <!-- Légende -->
                <Border Background="#1E293B" CornerRadius="8" Padding="15" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Légende" FontSize="14" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <material:PackIcon Kind="Car" Width="16" Height="16" Foreground="#48BB78" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Disponible" FontSize="12" Foreground="#48BB78"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <material:PackIcon Kind="Car" Width="16" Height="16" Foreground="#F56565" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Occupée" FontSize="12" Foreground="#F56565"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Pied de page -->
        <Border Grid.Row="2" Background="#252A41" Padding="15" Margin="0,20,0,0">
            <Grid>
                <TextBlock Text="© 2025 Smart Parking System" Foreground="#A0AEC0" 
                   FontSize="14" HorizontalAlignment="Left"/>
                <TextBlock Text="Version 2.5.1" Foreground="#A0AEC0" 
                   FontSize="14" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Page>